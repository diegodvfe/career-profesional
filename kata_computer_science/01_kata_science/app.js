

// hacer una función que reciba como parametro una pila, y un numero.
// La funcion debe de retornar  tantos elementos como diga el  numero(segundo parametro)
// Entrada:
// miFuncion([manaza, pera, durazno, sandia, melon], 2)
// Salida[manzana, pera]
let misFrutas = [manaza, pera, durazno, sandia, melon];

function totalElements(pila, numero) {
  const elment = [...pila];
  const result = [];
  
}
